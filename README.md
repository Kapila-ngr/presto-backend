# Simple Delivery Backend (Orders Only)

## Setup
1. Install dependencies: `npm install`
2. Configure MySQL in `.env`
3. Create database: `CREATE DATABASE simple_delivery;`
4. Build: `npm run build`
5. Run: `npm start` or `npm run dev`
